@model IEnumerable<Chatter.Models.Chat>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ChatID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ChatText)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ChatID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ChatText)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ChatText }) |
            @Html.ActionLink("Details", "Details", new { id=item.ChatText }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ChatText })
        </td>
    </tr>
}

</table>
